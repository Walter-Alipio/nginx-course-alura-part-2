server {
    listen 8005;
    server_name localhost;

    root /usr/share/nginx/performance;
    index index.html;

    gzip on;
    gzip_types text/css;

    location ~ \.jpg$ {
        expires 30d;
        add_header Cache-Control public;
    }
}
server {
    listen 8001;
    server_name localhost;

    location / {
        root /usr/share/nginx/servico1;
        index index.html;
    }
}

server {
    listen 8002;
    server_name localhost;

    location / {
        root /usr/share/nginx/servico2;
        index index.html;
    }
}
# NGINX: servidor Web, Proxy Reverso e API Getaway

Alura couse
- There are different branchs to each step of the course.

## Run
- The project was build using docker and docker compose, so you can donwload the project and use `docker-compose up -d` to start de container.
- Check available ports in your system, if need to change the project ports, you can do it easly editing `docker-compose.yml`